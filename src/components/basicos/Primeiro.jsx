import React from 'react';

export default function Primeiro() {
    let msg = 'Seja bem vindo(a)!';
    return (
        <div>
            <h1>
                Primeiro Componente
            </h1>
            <p>{msg}</p>
        </div>
    );
}